export type TUserServer = {
	_id: string; // firebaseId
	// _id: Types.ObjectId; // firebaseId
	// firebaseId: string;
	name: string;
	email: string;
	email_verified: boolean;
	// password: string;
	profilePicUrl?: string;
	status: string;
	// desc?: string;
	// createdAt: Date;
	// updatedAt: Date;
};
